- keyword = (params[:controller] == 'contents' && params[:action] == 'search')? params[:keyword] : nil

%form{:action => url_for(:controller => 'contents', :action => 'search')}
  %input{:type => 'text', :name => 'keyword', :value => keyword}
