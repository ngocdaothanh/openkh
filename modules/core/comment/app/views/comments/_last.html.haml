- # Renders the last comment. Used at a content's preview.
- # Input:
- #   comment

- dt = time_ago_in_words(comment.updated_at)
- key = (comment.updated_at > comment.created_at)? 'common.updated_ago' : 'common.time_ago'
- extra_row = t(key, :dt => dt)

.comments_last
  = html_user_info(comment.user, extra_row)
  .comment_message= comment.message
