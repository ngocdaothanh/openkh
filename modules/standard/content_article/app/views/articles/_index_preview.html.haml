- # Inputs:
- #   content

- article = content

%h2= link_to(article.title, article_path(article))
= html_content_header(article)

.article_introduction= sanitize(article.introduction)

= html_comment_last(article)
